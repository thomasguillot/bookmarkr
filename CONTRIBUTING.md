# Contributing

1. **Setup**: `npm install`
2. **Build**: `npm run build` (output in `build/`). Reload the extension in Chrome to test.
3. **Lint**: `npm run lint` / `npm run lint:fix`
4. **Icons**: Edit `icons/icon.svg`, then `npm run build:icons`

PRs welcome. Please run lint before submitting.
